# LaTeX Templates

This directory holds the LaTeX templates used by ApplyForge.

- `template.tex` — ATS-friendly CV/resume template (built in Stage 5)
- `cover_letter_template.tex` — Simple cover letter template (built in Stage 6)

Templates are populated server-side by the backend's LaTeX builder service
and compiled to PDF using `tectonic`.
